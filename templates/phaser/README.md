# Phaser template

Copy this folder into `games/{mechanic}-{orientation}`.

1. Set `index.html` title and `data-orientation` on `<html>`.
2. Wire `PlayScene` to the mechanic.
3. Import chrome from `@ui` (`shared/ui`).
4. `npm install && npm run dev`
5. `npm run build` → zip `dist` for itch.
